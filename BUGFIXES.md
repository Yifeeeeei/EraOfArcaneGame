# Bug Fixes Tracking

## 1. 统一卡牌查看逻辑 ✅

**问题**: 手牌双击查看 vs 技能牌单击查看，逻辑不统一

**解决方案**: 统一改为右键菜单

**改动文件**: `web/game.html`

**改动内容**:
- 移除手牌的 `@dblclick="showCardDetail(card)"`
- 移除技能池的 `@click="showCardDetail(card)" @dblclick="learnSkill(card)"`
- 统一添加 `@contextmenu.prevent="showCardContextMenu($event, card, 'source')"`
- 添加右键菜单组件（详情、学习、使用等选项）
- 添加 `context-menu` CSS 样式

## 2. 双击学习不工作 ⚠️

**问题**: 双击技能池中的技能无法学习

**原因分析**:
1. 前端发送了 `learn_skill` action
2. 但后端 **没有** 实现 `handleLearnSkill` 处理器
3. 另外，随着右键菜单的引入，双击逻辑已被移除，需要通过右键菜单学习

**解决方案**:
- 通过新的右键菜单"学习技能"选项来学习
- 或者需要在后端实现 `handleLearnSkill`

**临时解决方案**: 使用右键菜单 → 学习技能

## 3. 地形牌放到道具槽 ⚠️

**问题**: 地形牌(`is_terrain=true`)会放进道具槽而不是九宫格

**原因**: 道具的判断逻辑可能把地形也当成了普通道具

**需要检查**:
- `selectHandCard()` 中对道具的处理逻辑
- `isConsumable()` 函数是否正确排除地形
- `handleCellClick()` 中地形放置逻辑

**预期行为**:
- 地形牌应该高亮九宫格可放置位置
- 点击空格时调用 `placeTerrainCard()`

## 4. 掌门穆伶绝技目标检查 ⚠️

**问题**: 对方战场无伙伴时，绝技仍可发动但无效果

**卡牌**: 4311003 掌门穆伶

**绝技效果**: "选择法力范围内双方各1个伙伴，花费差值气，移回手牌"

**问题分析**:
1. 这是一个需要选择目标的绝技（双方各1个伙伴）
2. 当对方没有伙伴时，无法满足"双方各1个"的条件
3. 当前实现没有检查目标是否存在就允许使用

**解决方案**:
1. 在 `canUseUltimate()` 中检查是否有合法目标
2. 或者在使用时返回错误提示
3. 对于复杂的选择目标绝技，可能需要 PendingAction 系统支持

**当前状态**: 该绝技需要完整的目标选择系统支持，当前为简化实现

---

## 修复优先级

1. ✅ **高优先级**: 统一右键菜单（已完成基础修改）
2. 🔴 **高优先级**: 修复地形牌放置问题
3. 🟡 **中优先级**: 后端实现 handleLearnSkill
4. 🟡 **中优先级**: 绝技目标检查优化
